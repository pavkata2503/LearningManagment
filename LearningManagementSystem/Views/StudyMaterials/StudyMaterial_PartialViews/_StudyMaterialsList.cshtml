@* @model IEnumerable<AppData.Models.StudyMaterial>

@foreach (var material in Model)
{
    <article class="postcard light blue search">

        <a class="postcard__img_link" href="#">
            <img class="postcard__img" src="@material.URL" />
        </a>

        <div class="postcard__text t-dark">
            <h1 class="postcard__title blue"><a>@material.Title</a></h1>

            <div class="postcard__subtitle small">
                <time>
                    <i class="fas fa-calendar-alt mr-2"></i>@material.CreateDate.ToShortDateString()
                </time>
            </div>

            <div class="postcard__bar"></div>

            <div class="mat-des">

                <ul class="postcard__tagbox">
                    <li class="tag__item">@material.CreatedByName</li>
                    <li class="tag__item">@material.Subject</li>
                    <li class="tag__item play blue">@material.Class клас</li>
                </ul>

                <ul class="postcard__tagbox">

                    @if (User.IsInRole("Teacher"))
                    {
                        <li class="tag__item">
                            <a asp-action="Edit" asp-route-id="@material.Id" class="btn btn-secondary">Редактирай</a>
                        </li>
                    }

                    @* <li class="tag__item">
                        <a asp-action="Details" asp-route-id="@material.Id" class="btn btn-secondary">Прегледай</a>
                    </li> 

                    <a asp-controller="Tests" asp-action="Create" asp-route-materialId="@material.Id" class="btn btn-sm btn-info">Добави тест</a>

                    @if (User.IsInRole("Teacher"))
                    {
                        <li class="tag__item">
                            <form asp-action="Delete" asp-route-id="@material.Id" method="post">
                                <button type="submit" class="btn btn-secondary">Изтрий</button>
                            </form>
                        </li>
                    }

                </ul>

            </div>
        </div>
    </article>
}
 *@
@using AppData.Models.ViewModels
@model IEnumerable<StudyMaterialViewModel>

@if (!Model.Any())
{
    <div class="text-center py-5">
        <i class="bi bi-folder-x display-1 text-muted"></i>
        <h3 class="mt-3 text-muted">Няма намерени материали.</h3>
    </div>
}
else
{
    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
        @foreach (var item in Model)
        {
            <div class="col">
                <div class="card card-custom h-100 border-0">
                    <div class="card-body d-flex flex-column">
                        <div class="d-flex justify-content-between align-items-start mb-2">
                            <span class="badge rounded-pill bg-orange-light text-orange border border-warning">
                                @item.Category
                            </span>
                            <small class="text-muted">@item.CreatedOn.ToString("dd.MM.yyyy")</small>
                        </div>

                        <h5 class="card-title fw-bold text-dark text-truncate" title="@item.Title">
                            @item.Title
                        </h5>

                        <p class="card-text text-muted flex-grow-1 small">
                            @(item.Description != null && item.Description.Length > 100
                                                ? item.Description.Substring(0, 100) + "..."
                                                : item.Description)
                </p>

                        <div class="mt-3 pt-3 border-top d-flex justify-content-between align-items-center">
                            <div class="text-orange fs-4">
                        @if (item.TypeFile.ToString().Contains("Pdf"))
                                {
                                    <i class="bi bi-file-earmark-pdf"></i>
                                }
                                else if (item.TypeFile.ToString().Contains("Image"))
                                {

                                    <i class="bi bi-file-earmark-image"></i>
                                }
                                else
                                {

                                    <i class="bi bi-file-earmark-text"></i>
                                }
                            </div>

                            <div class="btn-group">
                                <a href="@item.FileUrl" download="@item.FileName" class="btn btn-sm btn-orange" title="Изтегли">
                                    <i class="bi bi-download"></i> Изтегли
                                </a>
                                <form asp-action="Delete" asp-route-id="@item.Id" method="post" class="d-inline" onsubmit="return confirm('Сигурни ли сте, че искате да изтриете този материал?');">
                                    <button type="submit" class="btn btn-sm btn-outline-danger ms-1">
                                        <i class="bi bi-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
}